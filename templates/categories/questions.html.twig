{% extends 'base.html.twig' %}

{% block title %}Utilisateurs{% endblock %}

{% block stylesheets %}
{% endblock %}

{% block body %}
    <div class="container">
        
        <table class="table table-dark table-striped">
            <h2 class="text-light text-center" >{{categorie.name}}</h2>
            <thead>
                <tr>
                    <th>Question</th>
                    <th></th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {% for quiz in quizz %}
                    <tr>
                        <td>{{ quiz.question }}</td>
                        <td><a href="{{ path('liste_reponses', { 'questionId': quiz.id }) }}">Reponses</a></td>
                        <td><a href="{{ path('edit_question', { 'questionId': quiz.id }) }}">Modifier</a></td>
                        <td><a href="{{ path('delete_question', { 'questionId': quiz.id }) }}">Supprimer</a></td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}